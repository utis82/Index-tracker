{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py create_admin && python manage.py collectstatic --noinput && gunicorn django_project.wsgi:application --bind 0.0.0.0:$PORT --timeout 300 --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}